<table class="scroll">
<% count=1 %>
<% @modules.each do |mod| %>	
		<% if count % 4 == 0 %>	<tr> <% end %>
				<td><%=  check_box_tag("model",mod.id, { :class => 'check_active':onclick => 'load_links(#{mod.id})' }) %></td><td><%= mod.name %></td>
		<% if count % 4 == 0 %>	</tr><% end %>
	<% count +=1 %>
<% end %>
</table>
<div id="load_links">

</div>


<script>
	$(".check_active").change(function () { 
		$("#load_links").load('/menu/links/load_links',function(){})
	 })

	function load_links(t){
		alert)t);
	}
		
</script>

<script>
/*
$('select_all').observe('click', function(event){
        $$('.corporate').each(function(i){
            $('service_groups_id_' + i.value).checked = $('select_all').checked;
        })
        var pars = "all_service_groups=" + $('select_all').checked;
        ajax_loader('clear_service', '/front_office/reports/load_service_for_group_wise', pars, 'image');
    })
    

 <%= check_box_tag 'service_group', c.id, checked=true, {:name => "service_groups[id][]", :id => "service_groups_id_#{c.id}",:onclick => "get_services('#{c.id}')",:class=>"corporate"} %>
                           
*/

</script>
