name: RailsWorkflow
on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Ruby
        # uses: ruby/setup-ruby@v1    
        run: echo "Setting up Ruby environment..."
        # with:
        #   ruby-version: '3.3' # Change to match your exact Ruby version
        #   bundler-cache: true # Automatically runs 'bundle install' and caches gems

      - name: Say Hello
        run: echo "Hello, Rails Workflow!"
        
    # Services are required if your Rails app relies on a database like PostgreSQL or MySQL
    # services:
    #   postgres:
    #     image: postgres:15
    #     env:
    #       POSTGRES_USER: postgres
    #       POSTGRES_PASSWORD: password
    #       POSTGRES_DB: rails_test
    #     ports:
    #       - 5432:5432
    #     options: >-
    #       --health-cmd pg_isready
    #       --health-interval 10s
    #       --health-timeout 5s
    #       --health-retries 5

    # env:
    #   RAILS_ENV: development
    #   RAILS_MASTER_KEY: ${{ secrets.RAILS_MASTER_KEY }}
    #   DATABASE_URL: "postgresql://postgres:password@localhost:5432/rails_test"

    # steps:
    #   - name: Checkout code
    #     uses: actions/checkout@v4

    #   - name: Set up Ruby
    #     uses: ruby/setup-ruby@v1
    #     with:
    #       ruby-version: '3.3' # Change to match your exact Ruby version
    #       bundler-cache: true # Automatically runs 'bundle install' and caches gems

    #   - name: Set up database
    #     run: |
    #       bin/rails db:create
    #       bin/rails db:schema:load

    #   - name: Run tests
    #     run: |
    #       bin/rails test # Change to 'bundle exec rspec' if using RSpec
